#include <bits/stdc++.h>
 
using namespace std;
 
int main()
{
    // amount of data unknown
    while (cin >> x) {
        // code
    }

    int N = 10;
    vector<int> numbers
    // amount of data known
    for (int i = 0; i < N ; i++){
        cin >> numbers[i];
    };  
}